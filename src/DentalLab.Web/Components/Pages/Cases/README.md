# Natural Dental Lab Cases UI

Replace the current files under `DentalLab.Web/Components/Pages/Cases/` with the files in this package.

Business logic and routes are preserved from the uploaded working files. The package standardizes the module styling and adds missing scoped CSS files for Details, Edit, Cancel, and RequestChange.

After replacement:

```powershell
dotnet clean
dotnet build
```
